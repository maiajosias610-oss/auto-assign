---
name: auto-assign
description: 通用任务派单与容量排期。按「类型×规模→工作量」规则表、执行者（人或 AI agent）的可用性与能力组、工作日历和现有占用，排成均衡排期，先出预览再写回飞书多维表格或导出 CSV。适用于设计/研发/运营/内容等各类团队的人人分工与 AI+人分工。当用户说派单、自动派单、排期、分配任务、均衡负载、算交付时间、写回飞书任务表时使用。
agent_created: true
---

# 通用任务派单（人 / AI 混排）

确定性排期引擎（不靠 LLM 拍脑袋），把任务清单变成「谁、哪天到哪天、做什么」，先出预览、确认后写回。

**不局限于设计师。** 执行者可以是人，也可以是 AI agent，两者混编在同一张成员表里，用同一套规则表与同一套排期算法。

## 什么时候用

- 一批任务要分给多人 / 多个 agent
- 需要按规则表算工作量、按容量排期、避免某个人堆满而他人空闲
- 需要区分「人受工作日与节假日约束」「AI 可 7×24」并混排
- 需要把结果写进飞书多维表格（或导出 CSV）

## 三步跑通

```bash
cp config/*.example.* config/   # 去掉 .example 后缀，改成自己的

python scripts/init_template.py --config-dir config --emit-fields
python scripts/plan.py --config-dir config --outdir out                      # 预览
python scripts/plan.py --config-dir config --outdir out --adapter feishu --apply
```

## 必须遵守的规矩

1. **任何写回前先出预览，拿到明确确认。** 写操作只在 `--apply` 下发生。
2. 数字对不上要主动披露，不许含糊。
3. @人、外发等外向动作，每次单独确认。

## 概念映射（换个部门就换这套词）

| 通用概念 | 设计团队 | 研发团队 | 内容团队 |
|---|---|---|---|
| 执行者 | 设计师 / 修图 agent | 开发 / coding agent | 撰稿 / 写作 agent |
| 能力组 | 视觉组 / 动效组 | 前端组 / 后端组 | 文案组 / 排版组 |
| 任务类型 | 礼物 / banner / icon | 需求 / 缺陷 / 重构 | 稿件 / 短视频脚本 |
| 规模（价值） | 蓝钻档位 / 经验值 | 故事点 / 人天 | 字数 / 篇 |

规则表 `workhours.csv` 的语义就是：**什么类型 + 什么规模 → 多少工作量 + 交给哪几个组 + 什么优先级**。

## 人 / AI 混排

成员表加这些列即可（字段名在 `field_map.members` 里改）：

| 列 | 取值 | 作用 |
|---|---|---|
| `类型` | `human` / `ai` | 决定工作日历 |
| `日容量` | 小时数，可留空 | 覆盖全局 `day_hours` |
| `模型` | 如 `gpt-5` | AI 具名用的模型名 |
| `并发数` | 整数，默认 1 | 同时能跑几个实例 |
| `日成本` | 元/天 | **人专用**：人天成本（× 工时） |
| `单次成本` | 元/次 | **AI 按次计费**：一次调用多少钱（与耗时无关） |
| `计费模式` | `按次` / `订阅` | AI 专用；留空默认按次 |
| `订阅费用` | 元/周期 | AI 订阅制：套餐费（月/年） |
| `订阅周期` | `月` / `年` | 与订阅费用配套 |
| `预估次数` | 整数 | 订阅期内预计跑多少次 → 单次 = 订阅费 ÷ 预估次数 |
| `本月已跑次数` | 整数 | AI 订阅制专用：本月已跑次数，用于累计「订阅费已摊」视图（留空=不累计） |

成本填写速查（给使用者填）：

- **人**：只填 `日成本`（如 `800`），其余成本列留空。`单次成本`/`计费模式`/`订阅*` 不用填。
- **AI 按次**：只填 `单次成本`（如 `0.35`），`计费模式` 写 `按次`（或不写），订阅三列留空。
- **AI 订阅**：填 `计费模式=订阅` + `订阅费用` + `订阅周期` + `预估次数`，`单次成本` 留空（引擎自动算 `订阅费÷预估次数`）。
  例：`订阅,68,月,200` → 单次 `0.34`。订阅费是固定成本，单次分摊只是估算口径，超出预估次数后边际成本趋近 0。
- **AI 订阅累计**（可选）：再填 `本月已跑次数`（如 `80`），`plan.md` 成本汇总里会自动给出「本月已跑 N 次 / 订阅费已摊 X 元（剩余 Y 元）」的累计视图；已跑超预估次数时提示「边际成本≈0」。

调度差异只在三处：

| | human | ai |
|---|---|---|
| 工作日历 | 跳周末、跳节假日 | **7×24，不受节假日影响** |
| 日容量 | `day_hours`（默认 8） | `日容量` 列，缺省用 `ai_daily_hours` |
| 并发 | 恒为 1（串行） | `并发数` N → 展开成 N 个调度槽，槽内串行、槽间并行 |
| 占用计算 | 按占用表实际数据 | 同左 |

三种典型分工模式：

1. **分工不重叠**——规则表候选链里人组和 AI 组并列，谁先有空给谁
   `设计组: 视觉组/AI初稿组`
2. **AI 打底 + 人收口**——用 `qty_shape: parent`（1 主任务 + N 子任务），父任务给人、子任务给 AI
3. **AI 只吃特定类型**——`workhours.csv` 里给该类型单独一行，候选组只写 AI 组
   `切图, 通配, 0.2天, AI批量组, P12`

注意：**同一个组里有多个 AI 时，按「最早空闲 → 负载最低 → 名字」挑**，不保证命中你以为的那个 agent。
要精确路由就给每个 agent 单独建组（`AI切图组` / `AI文案组`），别都叫 `AI组`。

### AI 具名实例

AI 不像人那样需要一个固定名字，可以按需生成具名实例。`schedule.yaml`：

```yaml
ai_naming: model_task   # off = 用成员表名 / model = 模型名 / model_task = 模型·需求名（默认）
```

| 模式 | 任务表「设计师」列显示 | 适用 |
|---|---|---|
| `off` | `初稿Agent` | 只要一个稳定标识 |
| `model` | `gpt-5` | 按模型统计 |
| `model_task` | `gpt-5·首页主banner` | 同一模型并行跑多任务，一眼看出哪条是哪个 |

输出里同时给 `执行者`（真实调度槽，如 `初稿Agent#2`）与 `模型` 两列，
展示名再花哨也不会混淆实际调度单位。写回飞书时 `设计师` 关联的是**基础名**
（槽位名 `初稿Agent#2` 在成员表里查不到，`resolve_links` 已用 label 处理）。

## 成本：人按天，AI 按次或订阅

人和 AI 的计费模型不同，别混为一谈：

- **人**：`日成本` × 工时(天)
- **AI 按次**：`单次成本`（一次调用多少钱，与耗时无关）
- **AI 订阅**：`订阅费用 ÷ 预估次数` 自动摊成单次；`订阅费用`/`预估次数`/`订阅周期` 三列配套填，`单次成本` 留空
- **规则表 `成本` 列**：优先级最高，适合"这类任务就是固定报价"的场景

单价优先级：**规则表 `成本` > 成员表 `单次成本`（订阅制已折算） > 成员表 `日成本` 折算**。

`plan.md` 末尾出「成本汇总」表：人力 / AI / 合计，并给出
**「这批 AI 任务若全改由人做要多少钱」**的参考差额——这是判断"该不该交给 AI"的依据。
订阅制 AI 会在汇总里单列一行说明摊销口径。没填单价的任务不计入合计，并在预览里提示。

## 交付约束：不要手调阈值

不要设「每人最多 12 天」这种固定值。改成配置业务目标：

```yaml
launch_date: 2026-09-15
finish_before_launch_days: 3   # 09-12 前全部收尾
```

引擎做法：**在 `[max_load_days_min, max_load_days_max]` 全区间扫描**，
逐个阈值正排一遍、算出收尾日，取**收尾最早**的那一档作为实际阈值，并在输出里报告它。

- **收尾日期对阈值不保证单调**，所以**不能用二分**。换个阈值会把任务重排到不同人身上，
  结果可能倒挂——实测出现过阈值 30 → 10-23 收尾、阈值 8 → 09-28 收尾。
  二分只会收敛到"恰好满足 deadline 的某个值"，而不是收尾最早的解
- 收尾相同时取**更小**的阈值（摊得更开、每人峰值更低）
- 扫描范围内全部超出 deadline：报警，但仍给出收尾最早的一版供人工决策，**不硬排**
- **别把下界设太小**：1~2 天会把同系列任务全打散，丢归属连续性。示例配置地板是 4 天
- 默认区间 1~30 天 = 30 次引擎调用；嫌慢就收窄 `max_load_days_min` / `max_load_days_max`

## 项目标识：用分类，别用标题前缀

不要用大写字母前缀（D01、C12）编码归属与状态——**不要把状态塞进名字**。
改成项目表两个独立字段：`项目分类`（单选）+ `是否完成`（标签）。

## 占用口径

- 占用 = 任务表「状态 ≠ 已完成 且 截止时间非空」的全部记录
- **无截止时间的任务不参与占用**（脏数据，别"顺手修复"）
- 本项目自身待重排的任务不计入占用（否则自己挡自己）

## 已知坑

- 飞书双向关联写入必须是**字符串数组** `["recX"]`，对象形式报 1254074
- 直连 PUT body 是 `{"fields": {...}}`，不是 MCP 包装格式
- 大表必须服务端 filter，`total` 不可信、全量拉取超时
- 数量解析：括号内尺寸 `座驾（600*300）` 不算数量，裸 `*N` 才是
- 排期是**快照**：方案隔周即过期，重跑比手改日期靠谱

详见 `references/feishu-api.md`。

## 文件索引

| 路径 | 说明 |
|---|---|
| `engine/planner.py` | 排期引擎（确定性算法，无 LLM） |
| `engine/fetch_holidays.py` | 节假日抓取 |
| `adapters/feishu_adapter.py` | 飞书读写（凭证走环境变量） |
| `adapters/csv_adapter.py` | CSV 后端（无飞书也能跑） |
| `scripts/plan.py` | 编排入口 |
| `scripts/init_template.py` | 需求模板 + 建表字段清单生成器 |
| `references/feishu-api.md` | 飞书接入细节与错误码 |

## 凭证

只走环境变量，不落盘、不进仓库：

```
FEISHU_APP_ID / FEISHU_APP_SECRET / FEISHU_APP_TOKEN
FEISHU_TABLE_TASK / FEISHU_TABLE_MEMBER / FEISHU_TABLE_PROJECT
```

## 可调参数（plan.py）

`--threshold N` 跳过自动求解 · `--adapter feishu|csv` · `--apply` 写回
`--active-value` 覆盖「可派状态」取值 · `--ai-daily-hours` 覆盖 AI 日容量
`--ai-naming off|model|model_task` 覆盖 AI 展示名 · `--currency` 成本单位符号
